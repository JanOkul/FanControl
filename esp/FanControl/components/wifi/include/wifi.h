/**
 * @brief Initalises the WIFI driver with credidentials found in main/WifiConfig.h.
 * 
 */
void initialise_wifi(void);

/**
 * @brief Creates an mDNS server for remote control of IR functions.
 * 
 */
void initialise_mdns(void);